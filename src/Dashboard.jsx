import React,{useState} from "react";
import SidePane from "./SidePane";

// will have all the logic

    const Dashboard = (props)=>{

    return(
        <div>
            <SidePane/>


        </div>
    )
}
export default Dashboard;